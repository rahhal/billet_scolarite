{% extends "base.html.twig" %}

{% block title %}{{ "Etablissement"|trans }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset("assets/css/style-crud.css") }}" rel="stylesheet" type="text/css">
{% endblock %}

{% block body %}
    <style>
        div.icon-spinner3 {
            font-size: 100%
        }

        .m-t-25 {
            margin-top: 25px;
        }

    </style>
      <div class="page-header page-header-default">
            <div class="breadcrumb-line">
                    <ul class="breadcrumb">
                                        <li><a href="index.html"><i class="icon-home2 position-left"></i> {{ "Données Statique"|trans }}</a></li>
                                        <li class="active">{{ "Etablissement"|trans }}</li>
                    </ul>
                    {% include "layout.html.twig" %} 
            </div>
    </div>
  
                    {# {{ include("AppBundle:StaticData:Etablissement/form.html.twig", {action:path("staticdata_etablissement_new")}) }} #}

                    {% set mode = form.vars.value is not null ? "edit" : "add" %}
{{ form_start(form,{attr : { action : path("etablissement_new") } } ) }}
{% if mode == "edit" %}
    <input type="hidden" id="id" value="{{ form.vars.value.id }}">
{% endif %}
<div class="row">
    {{ form_input("col-md-2","Ministère"|trans,form.ministere) }}
    {{ form_input("col-md-2","Mandoubia"|trans,form.mandoubia) }}
    {{ form_input("col-md-2","Code"|trans,form.code) }}
    {{ form_input("col-md-6","Nom"|trans,form.nom) }}
    {{ form_input("col-md-2","gouvernorat"|trans,form.gouvernorat) }}
    {{ form_input("col-md-2","Ville"|trans,form.ville) }}
    {{ form_input("col-md-4","Adresse"|trans,form.adresse) }}
    {{ form_input("col-md-2","Tél"|trans,form.tel) }}
    {{ form_input("col-md-2","Fax"|trans,form.fax) }}
</div>
<hr>

{# <div class="text-right">
    <button type="submit" class="btn btn-primary"><i
                class="fa fa-{{ (mode == "edit" ? "edit" : "save") }}  position-left"></i> {{ (mode == "edit" ? "Modifier" : "Enregistrer")|trans }}
    </button>
</div> #}
<div class="text-right">
    <button type="submit" class="btn btn-primary"><i
                class="fa fa-save position-left"></i> {{ "Enregistrer"|trans }}
    </button>
</div>
{{ form_end(form) }}
{# {% if mode == "edit" %}
    <script>
        $("#modal-edit-etablissement .select-search").select2();
        $("#modal-edit-etablissement .select").select2({minimumResultsForSearch: Infinity});


    </script>
{% endif %} #}

               
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ include("jquery_crud.html.twig",{"entity":"etablissement","pagination":true,"searching":true}) }}
{% endblock %}
