<!-- Main navbar -->
<div class="navbar navbar-inverse">
    <div class="navbar-header">
        <a class="navbar-brand" href="/"><h1>  منظـومـة القـيّم<h1></a>

    </div>

    <div class="navbar-collapse collapse" id="navbar-mobile">
        <ul class="nav navbar-nav">
            <li><a class="sidebar-control sidebar-main-toggle hidden-xs"><i class="icon-paragraph-justify3"></i></a></li>
        </ul>
        {% if is_granted('ROLE_ADMIN') %}
         <ul class="nav navbar-nav navbar-left">
            <li {% if app.request.get('_route') == 'home' %} class="active"{% endif %}>
                <a href="{{ path('home') }}"><i class="icon-home4"></i> <span>{{ "Billet d'entrée"|trans }}</span></a></li>

            <li {% if app.request.get('_route') == 'absences' %} class="active"{% endif %}>
                <a href="{{ path('absences') }}"><i class="icon-home"></i> <span>{{ "Punitions et absences"|trans }}</span></a></li>
        {% endif %}

        </ul>
        <ul class="nav navbar-nav navbar-right">
            <li class="dropdown language-switch hidden">
                <a class="dropdown-toggle" data-toggle="dropdown">
                    {% for locale in locales %}{% if locale.code == app.request.locale %}
                        <img src="{{asset('assets/images/flags/'~ locale.tag ~'.png')}}" class="position-left" alt="">
                        {{ locale.name }}
                        <span class="caret"></span>
                        {% endif %}
                    {% endfor %}
                </a>

                <ul class="dropdown-menu">
                    {% for locale in locales%}{% if locale.code != app.request.locale %}
                        <li><a href="{{ path('change_locale', {'locale': locale.code}) }}" class="deutsch"><img src="{{asset('assets/images/flags/'~ locale.tag ~'.png')}}" alt=""> {{ locale.name }}</a></li>
                   {% endif %}
                    {% endfor %}
                </ul>
            </li>

            <li class="dropdown dropdown-user">
                <a class="dropdown-toggle" data-toggle="dropdown">
                    <img src="{{asset('assets/images/placeholder.png')}}" alt="">
                    <span>{{ app.user.username }}</span> <i class="caret"></i>
                </a>

                <ul class="dropdown-menu dropdown-menu-right">
                   {#  {% if is_granted('ROLE_ADMIN') %}
                         {# <li><a href="{{ path('git_pull') }}"><i class="icon-loop"></i> <span>{{ "Recherche des mise a jour"|trans }}</span></a></li> #}
                        {# <li><a href="#"><i class="icon-loop"></i> <span>{{ "Recherche des mise a jour"|trans }}</span></a></li> #}

                        {# {% if not app.session.get('check-license') %}
                            <li>
                                <a href="javascript:void(0);" id="key_license"><i
                                            class="icon-key text-primary"></i><span>{{ "Clé License"|trans }}</span></a>
                            </li> 
                        {% endif %} #}
                        {# <li class="divider"></li>
                    {% endif %} #} 
                    <li><a href="{{ path('app_logout') }}"><i class="icon-switch2"></i> {{ "Logout"|trans }}</a></li>

                </ul>
            </li>
        </ul>
    </div>
</div>
<!-- /main navbar -->

